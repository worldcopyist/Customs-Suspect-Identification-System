# 开发过程记录（面向维护者）

本文供人类维护者阅读，记录项目从截图／演示页面走向当前 V1.2 可测试版本的主要问题、处理方式、证据与遗留风险。它和英文 [AI 开发交接手册](AI_DEVELOPMENT.md) 使用同一变更编号；英文手册提供给 AI 的完整技术索引，本文使用更容易阅读的中文说明。

更新日期：2026-09-08。最新 CHG-0017 仅更新目标文档，不升级当前程序。

## 先说明记录边界

初次建立记录时仓库没有 Git 提交历史；后来的发布与历史处理见 CHG-0012—CHG-0016。以下较早记录来自现有源码、实施说明、测试记录和用户反馈的回溯，能说明已知目标、处理方向和当前结果，但**不能还原每一次保存、每一行改动、每个精确时间或未保留的失败尝试**。因此，本文把“已核对”“历史记录”“待验证”分开，不把历史描述写成独立验收结论。

从本文件建立后，后续每一个独立修改都应先新增一个 `CHG-NNNN` 条目，写明问题、依据、原因或尚未确认的假设、修改过程、影响、实际测试结果、回退方式和遗留项；同时更新英文手册。不要删除旧条目，应以新的更正或替代条目说明变化。

## 文档关系

- [readme.md](../readme.md)：给使用者的项目介绍、启动、使用和常见问题。
- [AI 开发交接手册](AI_DEVELOPMENT.md)：给 AI 的纯英文上下文、源码映射、约束和变更模板。
- [V1.2 实施清单](V1.2_IMPLEMENTATION.md)：实现状态和测试证据。
- [实现边界](IMPLEMENTATION_BOUNDARIES.md)：模块位置、已实现和未验收事项。
- 原始 PRD、接口、技术架构和 AI 方案为受控本地资料，已按隐私要求排除在 Git 仓库外；需要据此修改时，应向项目负责人取得授权副本，并核对英文手册登记的版本和哈希。它们描述目标要求，并不自动证明已实现。

## 历史主要变更

### CHG-0001：建立 V1.2 全栈实现基础

- 状态：历史已实现。
- 问题：早期页面以视觉演示为主，前端、后端、数据持久化和真实业务流程不完整。
- 处理：以 FastAPI、SQLite、受控本地媒体和原生 ES Modules 建立同源系统，补充认证、用户、档案、检测、批次、摄像头、复核、聊天、云端配置、智能体、助手、日志和大屏模块，并使用 Alembic 迁移维护结构。
- 证据：现有源码、`docs/V1.2_IMPLEMENTATION.md` 和历史测试记录。
- 注意：原技术架构目标提出 Vue 3/Vite，但现有前端仍是原生 ES Modules；不能写成“架构完全对齐”。

### CHG-0002：修复首次进入错误跳转到改初始密码、登录注册加载失败和 CSRF 问题

- 状态：历史已实现。
- 问题：匿名打开服务时出现“修改初始密码”，登录和注册还曾显示 `Load failed` 或接口参数错误。
- 原因判断：匿名状态与“已验证但必须改初始密码”的受限会话未充分区分；前后端对同源 Cookie、CSRF 和错误返回的处理需要统一。该判断来自历史修复记录，不是本次重新复现的网络追踪。
- 处理过程：匿名用户进入登录页；只有用正确密码完成认证、且账户被标记为首次改密时，才进入受限改密页。前端集中处理 API 请求、CSRF 初始化、一次明确的 CSRF 失效恢复及请求编号提示。
- 影响：`ui/core.js`、认证接口、会话和安全服务。
- 历史验证：隔离登录、注册、改密和匿名入口检查通过。
- 遗留：尚未做生产环境下的登录限流和多浏览器长期会话验收。

### CHG-0003：认证页面、密码可见性和个人中心改密

- 状态：历史已实现。
- 问题：需要替换认证页左侧背景、删除装饰柱体、在密码框提供眼睛按钮，并让用户可在个人中心修改密码。
- 处理过程：认证页改为背景和文字组合；密码框右侧按钮可切换明文／密文；个人中心新增改密；密码不写入日志或文档。
- 规则冲突：早期反馈要求最少 8 位，后续接口文档明确统一新密码为 12～128 位，因此当前前后端按 12 位执行。已存在的密码哈希未因该变更被重置。
- 遗留：不同浏览器的完整视觉回归尚未验收。

### CHG-0004：登录状态持久化和失效行为

- 状态：历史已实现。
- 问题：用户希望登录状态持久化，但也曾出现因空闲过期的提示。
- 处理过程：使用服务端会话 Cookie，普通会话最长 8 小时、空闲 30 分钟；账户停用、角色／权限变化、改密和退出可撤销会话。后台轮询不应无限续期；退出请求未获服务端确认时，前端不假装已安全退出。
- 风险：持久化不是永久免登录，长时间、断网和多设备场景仍需专项验收。

### CHG-0005：清理示例数据并连接工作台、记录和人员名单

- 状态：历史已实现。
- 问题：工作台、检测记录、图片页出现示例记录、虚拟账户、演示数字和不一致表格；用户要求清零示例内容。
- 处理过程：已加载页面改读数据库和真实请求结果，移除被加载 UI 中的示例统计／检测记录；检测结果保留人工复核和人工关联历史。统计使用记录与检测框口径，不称作“识别人数”。
- 风险：不代表真实业务库已清理；不应为了清零页面而删除用户真实记录。

### CHG-0006：图片上传、批量检测和摄像头流程

- 状态：历史已实现。
- 问题：页面使用示例图片，用户需要点击／拖拽真实图片；摄像头停止按钮无反应。
- 处理过程：实现 1～10 张图片受控上传、逐项排队、取消、状态展示、原图与独立水印派生图、记录详情和人工复核；摄像头使用浏览器授权、暂停／恢复代际、停止轨道、过期帧丢弃和当前结果留档。
- 历史验证：真实 YOLO 子进程在两张合成图上完成推理并可读取媒体。该结果不证明模型识别精度。
- 遗留：实体摄像头授权、切换、断线恢复、跨设备 HTTPS 未验收。

### CHG-0007：补齐内部通讯、私聊、群聊与公共房间

- 状态：历史已实现。
- 问题：内部通讯页面消失；联系人无法点击私聊；建群只设 ID 或确认无反应；消息发送无效；联系人和群聊需要分区并限系统内部使用。
- 原因判断：早期页面事件和后端写入流程不完整；另有历史问题是通知异常会把已经提交的群聊写入误报成失败。
- 处理过程：补充私聊、管理员邀请建群、成员增删、群解散后只读历史、公共房间告知加入／退出、在线状态、文字和 Emoji、精确消息游标分页、成员范围内检索与 CSV 导出。通知失败与持久写入结果分离。
- 历史验证：隔离环境建群幂等、浏览器选择会话、发送并持久显示消息通过。
- 遗留：尚未完成同一局域网多设备部署和压力测试；项目不接外部通讯网络。

### CHG-0008：添加可维护日志与安全诊断

- 状态：历史已实现。
- 问题：用户希望为整个系统添加日志，方便定位群聊、登录和页面问题。
- 处理过程：加入请求编号、路由模板、关键审计／运维事件、受限浏览器诊断及轮转文件日志。运行命令关闭 Uvicorn 原始访问日志，减少搜索词或查询参数进入日志的风险。
- 边界：不记录密码、密钥、会话 Cookie、请求正文、完整私人聊天正文、原始搜索 URL 或堆栈。
- 遗留：数据库运维日志没有自动保留期清理，需要容量策略和备份恢复演练。

### CHG-0009：补齐大模型、智能体和数字人页面

- 状态：历史已实现。
- 问题：LLM 设置、数字人和聊天窗口消失；配置块溢出、发送按钮无反应；希望管理员管理 API、温度和数字人资产。
- 处理过程：增加厂商配置、加密保存 API Key、显示掩码、显式能力测试、智能体版本、助手对话和确认发送；发送前展示实际外发文本，确认后最多一次调用。数字人页面保留成年男性图片占位并支持受控本地自包含 GLB 渲染与失败降级。
- 边界：测试成功不会自动启用厂商；网页不允许任意 3D 上传；当前没有正式 GLB。真实厂商、费用和 3D/GPU 验收均未完成。

### CHG-0010：助手回答事实约束与引用撤权

- 状态：历史已实现。
- 问题：报告／摘要不能凭空编造检测事实，需要用户查看来源，来源权限变动后不能继续展示失效内容。
- 处理过程：`customs-text-v3` 由后端冻结检测事实，业务模式只接受严格结构化结果；引用仅可指向有权访问的记录或资料块；查看引用和展示历史时重新检查所有权、版本、状态和内容哈希；模型输出工具调用被拒绝。
- 历史验证：离线测试覆盖不合法结构、工具调用、引用与撤权。
- 重要限制：这些规则降低编造风险，不证明大模型对资料的自然语言解释正确，仍必须人工核验。

### CHG-0011：建立人类 README、AI 开发交接和中文开发记录

- 状态：完成，2026-09-07。
- 问题：用户要求每次后续修改前，AI 能通过项目文档了解需求原件、当前实现、历史问题和修改过程；同时需要给人阅读的中文项目说明和中文开发记录。
- 处理过程：新增根目录 `readme.md`、`AGENTS.md`、`docs/AI_DEVELOPMENT.md` 与本文；登记原始 PRD／接口／架构／AI 文档及其指纹，列出当前代码结构、边界、历史修改、验证证据、待办和后续记录模板。
- 影响：仅新增文档，不修改运行代码、数据库、账户、密码、媒体、模型或密钥。
- 验证：2026-09-07 已检查原始文档链接和中英文变更编号；英文手册仅含 ASCII 文本；`npm run check` 通过。本次只新增文档，没有重新运行完整业务测试。
- 回退：日后可通过版本控制只撤回四份文档，不需要数据恢复。

### CHG-0012：安装 GitHub CLI 并首次发布仓库

- 状态：完成，2026-09-07。
- 问题：需要把本地项目上传到 `worldcopyist/Customs-Suspect-Identification-System`，但直接 HTTPS 推送没有可用凭据。
- 已核实事实：GitHub 插件确认该账户具备仓库写入权限，但没有“完整上传本地工作区”的接口；文本 API 和内联 Base64 Blob API 不适合无遗漏地传输项目中的模型和图片。远端最初只有 `31f8015` 初始提交和根目录 `LICENSE`，本地此前没有 Git 提交历史。
- 处理过程：从 GitHub 官方发布包安装 macOS arm64 的 GitHub CLI 2.100.0，并将下载包的 SHA-256 与官方校验表一致性核对；让 Git 使用已认证的 CLI 凭据；保留远端许可证并合并历史；使用非强制推送上传。
- 结果：远端 `main` 从 `31f8015` 更新为 `a531734d2fdfa6c08d2da9275f45d6990f9a5ee1`。项目首个提交为 `bc9683859eb77edcaca133725061ced8afa5de97`，合并提交保留远端初始历史。
- 影响：安装和推送不改动运行源码、数据库、媒体、模型或密钥；本条及英文对应记录是本次新增的项目文件变更。
- 验证：GitHub CLI 显示已认证的 `worldcopyist` 账户；非强制推送完成；GitHub API 查询和本地 `origin/main` 均指向相同合并提交。
- 安全：Token 没有写入项目、日志或本文档，CLI 凭据由系统凭据存储保管。
- 回退：如需撤回，请先评估是否已有协作者或后续提交；不要为了回退而强制推送或删除远端分支。

英文对应项：`docs/AI_DEVELOPMENT.md#chg-0012`。

### CHG-0013：仓库隐私排除与发布内容审计

- 状态：完成，2026-09-07。
- 用户需求：停止上传指定的本地页面效果图、页面素材、原始需求文档目录和其压缩包，并检查即将上传的内容是否含姓名、Token、API 凭据等隐私信息。
- 已核实事实与边界：开始前远端 `main` 和本地 `master` 都指向 `4ba58e3`；四个目标目前均已被 Git 跟踪。仅写 `.gitignore` 不会影响已跟踪文件，必须在不删除本地文件的情况下从 Git 索引移除。索引移除只能让后续版本树不再包含它们，不能删除旧提交中的文件、路径或内容；改写历史和强制推送尚未获得授权。
- 处理过程：增加根目录、且不包含个人姓名的忽略规则；使用 `git rm -r --cached` 从 Git 索引移除 24 项指定资料，并确认它们在本机仍存在；把公开文档中指向受控本地资料的链接改为版本／哈希登记说明；扫描当前待发布索引和全部可达 Git 历史中的常见真实凭据特征，并检查仍会发布的 PNG 元数据字段。
- 影响：只涉及 Git 跟踪规则和维护文档；不删除本地原始资料，不修改运行代码、数据库、账户、密码、Token、模型或媒体。
- 验证：`git fetch origin main` 后，远端和本地开始时均为 `4ba58e3`；四类路径均命中忽略规则，当前索引已无这些路径，暂存删除数为 24，且本机目标都保留；`git diff --cached --check` 通过。当前索引与全部可达历史均未命中所扫描的 GitHub、OpenAI 风格、AWS、Google、Slack 或私钥格式。关键词命中仅为配置占位符、安全实现、已公开说明的初始化／开发口令和隔离测试口令，不能据此认定存在已部署真实密钥。两张保留 PNG 均未含 Artist、Copyright、GPS 或 UserComment EXIF 字段；英文手册 ASCII 检查和受控来源链接检查通过。
- 回退：提交前可恢复暂存状态；提交后应通过新的非强制提交恢复，不应为了回退而删除本地资料或强制推送。
- 遗留风险：这是基于特征的工程审计，不是取证或法律隐私认证。旧 Git 历史仍含已排除资料及其历史路径；如需彻底从远端历史移除，必须先明确授权历史重写并评估协作者影响。对外部署前还必须把开发应用密钥占位符、文档和测试中的口令替换为受管密钥；该运行安全改动不属于本次授权范围。
- 英文对应项：`docs/AI_DEVELOPMENT.md#chg-0013`。

## 后续已记录变更
### CHG-0014：主题切换与企业演示品牌

- 状态：完成，2026-09-07。
- 需求：新增浅色、深色、跟随系统及偏好保存，产品改为企业演示定位。
- 原因：工作区硬编码深色、认证页硬编码浅色，前后端生成文案残留旧标签。
- 过程：新增全局主题入口、首屏初始化、系统变化监听；更新界面和新生成报告、水印；随后隔离测试。
- 边界：保留现有数据库路径、账户、历史媒体及历史开发条目；用户新要求替代旧品牌要求，人工复核边界保留。
- 结果：全局右上角入口覆盖认证和业务页面，默认跟随系统；本地存储保存设置、同源标签页同步，存储被禁用时可继续切换。已更新前端、旧演示脚本、API 标题、助手提示和新生成水印／报告，并为四份原始设计文档追加用户修订和核对新哈希。保留历史迁移、历史媒体、用户自定义内容与数据库内部名称。
- 验证：40 项隔离后端测试通过（有两项依赖弃用警告）；npm run check 及主题事件测试通过。浏览器核验登录／注册页浅深色、深色刷新保留和实际背景颜色；系统变化与存储受限通过隔离事件测试验证，未更改本机系统外观。已登录业务页面的完整视觉验收留待用户测试。服务已重启于本机 8000 端口。
- 影响：新增 ui/theme.js、ui/theme.css、scripts/check-theme.mjs，修改入口、UI 模块、后台输出文案及维护说明；无接口字段或数据库迁移，主题按浏览器保存。回退仅撤回 CHG-0014 改动。

### CHG-0015：替换用户提供的 YOLO 权重并预览登录前动画

- 状态：完成，2026-09-08。
- 需求：使用用户提供的新 `best.pt` 替换当前本地 YOLO；将两个 HTML 及随后补充的图片作为未登录页面前的启动动画，用户不满意时可以回退。
- 已核实的兼容性变化：旧权重的 SHA-256 为 `6f4a8baed78f970a5141cc43e356af8f68c8cef6e7e73ab376ea6893ed4a2b1a`，唯一类别是 `sus`；新权重的 SHA-256 为 `d8387eb6ed98d6ff13013f8dc5b1eaac87cb243dc9aa8bc931adf2789abe9329`，可作为 Ultralytics 检测模型加载，但唯一类别是 `handsome`。当前推理进程会严格拒绝类别变化，因此只复制新文件会使检测不可用，必须同步修改受控类别契约和展示文案。模型加载只能证明格式可用，不代表识别精度、类别业务含义、真实摄像头效果或适用性已经验收。
- 动画资源：两个 HTML 本身没有外链或脚本调用，但原本引用同级 `image/` 目录；用户已补充该目录。计划使用同源、受限 iframe 展示，且只在匿名登录前播放并提供跳过，不改变登录成功后的业务入口。
- 边界与回退：不新增网页模型上传、阈值调参或自动训练入口；不改数据库、账户、历史媒体和网络监听。旧权重会保留为不纳入 Git 的本地校验备份；如需回退，只恢复该备份并撤回本条源码和动画资源，不删除新模型期间已经生成的历史记录。
- 实施结果：旧模型已保存在被 Git 忽略的 `backend/models/backups/suspect-yolo11n-best-6f4a8baed78f.pt`，新权重以原子替换方式安装。固定哈希、推理子进程类别契约、结果框标签和助手事实说明均切换至 `handsome`；不提供任何网页模型上传、激活或调参功能。两个动画页面与用户补充的五张图片已放入同源静态资源路径，匿名启动时按顺序播放并可跳过，登录成功后的业务刷新不播放。由于原有全局 `frame-ancestors 'none'` 会阻止动画 iframe，已只允许 `/ui/intro/` 下无脚本静态页面被本站嵌入，其余页面仍禁止被嵌入。
- 验证：新文件哈希与配置一致；Ultralytics 实际加载为 `detect`、类别 `{0: handsome}`，合成空白图真实推理返回 0 框。模型就绪检查通过；`npm run check` 通过并新增动画无脚本、无外链、资源完整和匿名登录顺序检查；40 项后端隔离测试通过（两项依赖弃用警告）。健康接口、两张动画 HTML 和代表性图片均返回 HTTP 200；浏览器实测两段动画可见并会进入登录页。首次浏览器测试曾被 CSP 阻止，修正为上述最小同源例外后复测通过。
- 仍未验收：没有评价模型精度、偏差、训练来源、真实摄像头、物理设备或真实业务适用性；用户对动画视觉效果的主观验收仍需自行确认。不满意时仅回退 CHG-0015，不删除新模型运行期间的历史数据。
- 英文对应项：`docs/AI_DEVELOPMENT.md#chg-0015`。

### CHG-0016：发布当前工作并从远端历史移除私有路径

- 状态：完成，2026-09-08。
- 需求：用户要求把项目上传到其 GitHub 仓库，且只上传要求上传的文件；在被告知 CHG-0013 只是把私有路径移出最新文件树、旧提交仍公开暴露这些内容后，用户明确授权改写全部历史并强制推送。
- 事实：作业开始前本地 `master` 与 `origin/main` 均为 `4ba58e3`。工作区包含 CHG-0014／0015 代码、已批准的新模型、动画资源、CHG-0013 的索引删除，以及八个此前未跟踪的运行文件（主题、动画、自检脚本、五张启动图）。五张启动图被服务中的 `/ui/intro/` 页面引用，属于应用运行所需，不在用户排除的私有路径内。补充发现：CHG-0013 漏掉了 `前端页面效果图-18张PNG.zip`（约 21 MB，自首个项目提交起就被跟踪），本条更正该遗漏。
- 过程：提交全部应上传变更（13 项新增、27 项修改、24 项移出索引；凭据签名扫描无命中；`npm run check` 全部通过）；创建完整 bundle 备份 `~/suspect-repo-backup-before-rewrite-20260908.bundle`；安装 git-filter-repo；两次重写全部提交，移除 `day4_1_*`、两个中文私有目录和漏掉的压缩包；重新添加被 filter-repo 按设计移除的 `origin`；以 `--force-with-lease` 锁定观测到的远端值强推 `main` 两次。
- 边界：只改动版本历史与本地仓库；不改运行时接口、数据库、账户、凭据、媒体或模型。本地私有文件保留在磁盘并继续被忽略。
- 验证：每次重写后用关闭 `core.quotePath` 的逐提交 `ls-tree` 核对，私有路径命中数为 0；重写后 `HEAD` 含 127 个文件，`LICENSE` 保留，模型二进制 SHA-256 仍与 CHG-0015 批准值一致；通过 GitHub API 逐个核对远端可达提交的树，私有路径命中数均为 0，远端 `main` 为最终重写提交。首次漏检的原因是 Git 默认对非 ASCII 路径做八进制转义，普通文本 grep 未命中，已改用 `core.quotePath=false` 复核。
- 风险与回退：强推改变了 `351595b` 之后的全部提交哈希，其他旧历史克隆必须重新克隆；GitHub 缓存中的旧对象可能短暂残留，必要时联系官方支持清理。回退可克隆改写前的 bundle 备份恢复原历史。
- 英文对应项：`docs/AI_DEVELOPMENT.md#chg-0016`。


## 历史遗留事项（不纳入本轮材料或验收工作）

以下原有事项保留历史上下文；第1项已由 CHG-0017 在设计层明确沿用原生 ES Modules，其他事项不在本轮执行。

1. Vue 3/Vite 架构迁移，或在原始架构文档中明确改为原生 ES Modules。
2. 在用户批准的真实凭据和预算下验收 DeepSeek／千问等云端调用以及新结构化输出。
3. 提供有许可的正式、自包含 GLB，并在目标 GPU／浏览器测试资源预算和降级。
4. 用实际摄像头、可信 HTTPS 和多台设备完成局域网验收。
5. 制定日志保留、磁盘容量、媒体清理、完整备份和恢复流程。
6. 完成 Windows、并发、重启、故障注入和长时间稳定性测试。
7. 加强厂商能力测试的全局并发／费用控制，补足 DNS 到实际连接的一致性防护。

<a id="chg-0017"></a>
### CHG-0017：任务书功能与多模态、JWT、数字人管理文档修订

- 状态：完成（仅文档）；日期：2026-09-08。
- 用户范围：只修改文档，不实施代码、迁移目录或替换模型；不制作交付材料，不开展验收证据工作。明确纳入 JWT、厂商模型列表自动填充、语音和多模态接口、管理员／超级管理员更换数字人、database/results 目录及其余功能缺口。
- 依据：本次明确要求、任务书第 1—5 页功能条款、四份受控设计文档及 CHG-0014／CHG-0015 后续修订。
- 已核实事实：当前实现仍为不透明 Cookie 会话、文本助手和文本聊天、部署配置 GLB、backend/data 与 backend/media。用户资源含两套 PMX 2.0、贴图和使用说明，不是 GLB；说明禁止商业用途和二次配布，不能把企业演示定位视为使用授权。
- 更正 CHG-0015：该条所称类别展示全部同步并不完整，ui/detection.js 的摄像头框与未检出文案仍写死 sus；本次只登记待修复，不改代码或历史检测记录。
- 过程：先读开发文档、原始需求和实际调用链；四份原件指纹与登记一致；只读检查资产头信息和说明；查询厂商目录、多模态和 JWT 官方资料；为四份原件增加优先级明确的当前有效章节，保留历史基础条款；同步根／后端 README、实施状态和开发手册。
- 影响：四份原始设计、根／后端 README、实施清单、实现边界、中英文开发记录；接口、权限、数据迁移均只形成设计，不实际生效。
- 检查过程与失败记录：首轮文档检查发现接口新增表格两处未转义分隔符，已修正；一次源码路径查询误用 backend/app/web/ui，随后定位实际 ui/detection.js；DeepSeek 官方页首次超时、猜测的千问目录地址不可用，随后通过官方搜索定位正确来源。最终检查通过：核对10份关联文档、27个本地链接、17个中英文一致变更编号、4份受控原件新指纹、Markdown 表格／代码围栏／锚点、英文手册 ASCII、仅文档差异及 git diff --check。未运行业务测试或云端推理。
- 回退：`/tmp/customs-docs-v13.3NnOip/before-contract-edit.tar` 保留合同正文修改前10份文档，此时已经有本条“进行中”记录。仅恢复需要撤回的需求／说明文字，以后续记录说明回退，不抹除历史；备份含受控原件，禁止公开发布，不涉及业务数据恢复。
- 遗留：功能实施另行开展；数字人实际启用需完成格式准备及许可确认，不推定商业或分发权。
- 英文对应项：[CHG-0017](AI_DEVELOPMENT.md#chg-0017)。

<a id="chg-0018"></a>
### CHG-0018：迁移用户提供修改版压缩包中的适用修复

- 状态：完成；日期：2026-09-08。
- 用户需求：检查 `Customs-Suspect-Identification-System-main.zip` 中的修改，将适用的部分迁入原项目，并总结修改内容。
- 已核实事实：压缩包 SHA-256 为 `6cb3ae84eb91f3d3a86554b8114bbe258308d02e326d53929df7e76ec99ff4dc`，共227项，含 SQLite 数据库、运行日志和 Python 字节码缓存。这些是生成的运行状态，不是源码修改，不能覆盖原项目数据。源码差异涉及认证退出、中间件、LLM、管理端温度输入、前端错误文案、SQLite 版本门槛、文档和新增 Windows 启动脚本；压缩包没有当前受控 V1.3 原件，内含文档早于 CHG-0017。
- 判断与结果：迁入退出响应修复、完整的中间件状态码健壮性、非流式千问思考控制和 Windows 启动入口；不迁入 SQLite 最低版本从 3.51.3 下调至 3.51.1、移除前端请求编号、管理端温度范围从 0–2 缩为 0–1、生成物或旧文档。前两项排除会削弱既有运行／排障契约，温度缩小缺少相应后端和文档依据。压缩包中间件修复仍保留一次空状态码直接比较，已在原项目统一改用安全状态码变量，而非原样复制。
- 修改过程：退出接口保持声明的 204 空响应并清除 Cookie；请求日志／审计中间件以安全状态码处理空值；非流式 `qwen3*` 请求显式关闭思考模式，以对齐既有流式路径；新增 `backend/start.bat`，优先使用 `backend/.venv` 的解释器并只经 `app.main` 监听本机回环地址；新增无网络 LLM 请求载荷回归测试。新测试首次被放在前一测试的清理块之前，静态检查发现残留缩进后已在执行测试前修正。
- 影响范围：认证退出、日志／审计中间件、LLM 请求载荷、Windows 本地启动、测试与运行说明；未导入压缩包数据库、日志、缓存、模型、密钥、账户、媒体或受控设计原件。未改变 API 路由、权限模型、数据表或 SQLite 基线。
- 验证：`PYTHONPATH=backend backend/.venv/bin/python -m pytest backend/tests/test_auth_and_users.py backend/tests/test_health.py -q` 为 5 通过、2 条依赖弃用警告；新增 LLM 测试和全部测试以最终记录为准；`PYTHONPATH=backend backend/.venv/bin/python -m pytest backend/tests -q` 为 41 通过、2 条依赖弃用警告；`npm run check` 的5项检查通过；`py_compile` 和 `git diff --check` 通过。测试使用隔离数据库；未调用云端、未改业务库、未发布服务。Windows 脚本仅静态审阅，尚未在 Windows 真机运行。
- 回退：仅撤回本条选择的源码、测试、启动脚本和说明；新增启动脚本可单独移除。不得用压缩包的数据库／日志／缓存回退原项目。
- 英文对应项：[CHG-0018](AI_DEVELOPMENT.md#chg-0018)。

<a id="chg-0019"></a>
### CHG-0019：实施 V1.3 基础能力并修补现有问题

- 状态：完成（V1.3 部分基础）；日期：2026-09-08。
- 用户需求：按修订后的项目文档修改项目，补齐缺失功能、修补问题并更换登录方式。
- 依据：PRD V13-01—V13-08、接口 C13-00—C13-09、架构 A13-01—A13-09、AI 方案 AI13-01—AI13-10，以及用户本轮明确请求。
- 已核实事实与范围控制：当前仍为 V1.2 不透明 Cookie、`backend/data`／`backend/media`、纯文本助手和部署 GLB 读取入口；目标是 JWT、数据根、语音／多模态、资产管理等不兼容契约。不能通过重建现有数据库完成迁移，也不能把许可未确认的 PMX 资源复制进仓库。先实施可核验、可迁移的基础和已知问题修补；真实厂商调用、资产启用和真实业务库迁移不因本条自动获得授权。
- 修改过程：新增 `20260908_0007` 迁移，为 User／AuthSession 增加认证版本并撤销旧不透明会话；登录改为本地 HS256 JWT 写入 HttpOnly `customs_access`，签名密钥独立于 `APP_SECRET_KEY`，令牌含 issuer、audience、sub、sid、jti、时限、auth_version 与 phase；REST／WebSocket 仍逐次回查当前 sid、账户状态和认证版本。成功新登录撤销同账号旧 sid；改密、角色、权限、启停、删除和重置递增认证版本并撤销旧会话；匿名 CSRF 与身份 JWT 分离，失效令牌会被清理后允许重新登录。默认路径改为 `database/`／`results/`，增加显式初始化脚本，正常启动缺库不再自动创建 admin；摄像头显示改用服务端 class_name，不再写死 sus。
- 未实施：厂商模型目录和表单自动填充、VISION／ASR／TTS／OMNI、截图聊天附件、数字人资产版本／PMX 转换、detect_log 物理迁移、真实旧目录数据／媒体搬迁。这些需要独立数据模型、厂商协议、受控转换、许可确认和维护窗口，未用占位接口假装可用。
- 验证：认证定向测试 3 项通过；全部后端隔离测试 43 项通过（2 条上游依赖弃用警告）；`npm run check` 5 项通过；修改的 Python 文件编译和 `git diff --check` 通过。未调用真实厂商、摄像头硬件、PMX、业务数据库、局域网服务或 Windows 真机。
- 安全／数据边界：未重置密码、未轮换既有加密密钥、未发布服务、未调用付费厂商、未导入用户 PMX、未迁移业务数据。旧浏览器会话按契约被撤销；账户、密码哈希、厂商密钥和业务记录保留。
- 回退：认证迁移不支持直接降级，应先恢复已核验数据库备份，再撤回本条应用／配置／前端／文档修改；不得恢复 V1.2 浏览器令牌或用压缩包运行数据覆盖当前库。
- 英文对应项：[CHG-0019](AI_DEVELOPMENT.md#chg-0019)。

## 后续记录模板

```markdown
### CHG-NNNN：简短标题

- 状态：进行中／已完成／受阻／已替代
- 日期：YYYY-MM-DD
- 用户问题或需求：
- 依据：PRD／接口／架构／AI 文档章节，或明确的新需求
- 已核实事实、推测和信息缺口：
- 原因判断：
- 修改过程：
- 影响文件、接口、数据、权限和安全边界：
- 实际验证命令与结果：
- 回退或恢复方式：
- 遗留风险、外部前置条件和后续编号：
- 英文对应项：`docs/AI_DEVELOPMENT.md#chg-nnnn`
```

文档维护不能替代版本控制。建议后续在获得维护者同意后，为每个可交付变更建立 Git 提交、写清测试结果，并在修改数据库前执行可恢复备份。
